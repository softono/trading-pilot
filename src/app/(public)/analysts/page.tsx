import AnalystListPublicPage from "@/modules/analyst/AnalystListPublicPage";

export const metadata = {
  title: "Analysts",
  description:
    "Browse AI and expert analysts sharing live trading signals — subscribe free to unlock entry, stop, and targets.",
};

export default function Page() {
  return <AnalystListPublicPage />;
}
